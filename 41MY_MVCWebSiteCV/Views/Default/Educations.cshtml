@using _41MY_MVCWebSiteCV.Models.Entity
@model List<Tbl_Education>

<div class="resume-section-content">
    <h2 class="mb-5">EĞİTİM HAYATIM</h2>
    @foreach (var x in Model)
    {
        <div class="d-flex flex-column flex-md-row justify-content-between mb-5">
            <div class="flex-grow-1">
                <h3 class="mb-0">@x.Title</h3>
                <div class="subheading mb-3">@x.SubHeading</div>
                <div>@x.SubHeading2</div>
                <p>GANO: @x.GPA</p>
            </div>
            <div class="flex-shrink-0"><span class="text-primary">@x.Date</span></div>
        </div>
    }
</div>

